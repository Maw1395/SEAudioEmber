<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.fsu.cs.mobile.songdatabase">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Added the other 6 activities -->
        <activity android:name=".Month_Selection" >
            <intent-filter>
                <action android:name="android.intent.action.MONTH_SELECTION" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Genre_Selection" >
            <intent-filter>
                <action android:name="android.intent.action.GENRE_SELECTION" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Month_Year_Result" >
            <intent-filter>
                <action android:name="android.intent.action.MONTH_YEAR_RESULT" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Song_Result" >
            <intent-filter>
                <action android:name="android.intent.action.SONG_RESULT" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Song_Artist_Search" >
            <intent-filter>
                <action android:name="android.intent.action.Song_Artist_Search" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Song_Artist_Search_Result" >
            <intent-filter>
                <action android:name="android.intent.action.SONG_ARTIST_SEARCH_RESULT" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- End of added activities -->

    </application>

</manifest>